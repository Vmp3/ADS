
/**
 * exibir dados no container  
 * @param {object} children pega os dados entre o nome do componente 
 * @returns {HTMLElement}elemento html com os dados
 */

const Section = ({children}) =>{

    return <section>{children}</section>

};

export default Section;